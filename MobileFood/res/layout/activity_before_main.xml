<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <Button
        android:id="@+id/before_main_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Disclaimer" />

    <ListView
        android:id="@+id/before_main_listView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/before_main_button"
        android:layout_below="@+id/before_main_text" />

    <TextView
        android:id="@+id/before_main_text"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:textSize="16dp"
        android:textStyle="bold"
        android:text="Bitte wählen Sie eine zugehörige Gemeinde aus. Beachten Sie, dass Sie diese Wahl nur einmal treffen können!" />

</RelativeLayout>